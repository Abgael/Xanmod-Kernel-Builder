If you are willing to build and install your own Xanmod kernel, use the standalone installer so that you can copy the config file and customize it later. You will also not have problems with kernel updates overwriting your own local built kernel.
If you just want to use Xanmod as is, you can use the repository installer.
Either installer you choose, remember to NOT run this script as root.
