If you are willing to build and install your own Xanmod kernel, use the standalone installer so that you can copy the config file and customize it later.
If you just want to use Xanmod as is, you can use the repo installer.
Either installer you choose, remember to NOT run as root.
